'use client';
import { styled } from '@mui/material/styles';

export const PageHero = styled('header')(({ theme }) => ({
  height: 640,
  textAlign: 'center',
  display: 'flex',
  alignItems: 'center',
}));
